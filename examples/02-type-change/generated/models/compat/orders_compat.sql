{{ config(materialized='view', tags=['fuse','deprecation']) }}

-- Generated by Fuse. Keeps `order_total` available while consumers migrate:
--   - order_history
--   - ORDER_HISTORY
--   - Customer Analytics Measures
--   - Essential KPI Measures
--   - Geographic Measures
--   - Product Perfromance Measures
--   ... and 6 more
--
-- Drop this view once none of them reference `order_total`. Every column below was
-- confirmed against DataHub at generation time.

select
    billing_address_id,
    cost_of_delivery,
    customer_id,
    delivery_address_id,
    delivery_type,
    order_date,
    order_id,
    order_mode,
    order_status,
    payment_method_code,
    promotion_id,
    sales_rep_id,
    wait_till_complete_yn,
    warehouse_id,
    null as order_total  -- deprecated: removed upstream, preserved as NULL
from {{ ref('orders') }}
