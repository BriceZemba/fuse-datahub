{{ config(materialized='view', tags=['fuse','deprecation']) }}

-- Generated by Fuse. Keeps `credit_limit` available while consumers migrate:
--   - customer_churn_model
--   - prod-retention-service
--   - customer_churn_features
--   - customer_churn_models
--   - order_details
--
-- Drop this view once none of them reference `credit_limit`.
-- Every column below was confirmed against DataHub at generation time.

select
    account_mgr_id,
    address_line1,
    address_line2,
    address_line3,
    country_id,
    cust_email,
    cust_first_name,
    cust_last_name,
    customer_class,
    customer_id,
    customer_since,
    dob,
    mailshot,
    nls_language,
    nls_territory,
    partner_mailshot,
    phone_number,
    region_id,
    suggestions,
    town_city,
    zipcode,
    null as credit_limit  -- deprecated: removed upstream, preserved as NULL
from {{ ref('customers') }}
