# Convenience mirror of pyproject.toml dependencies for `pip install -r`.
# `pip install -e .` is the supported path; this exists so a judge in a hurry can
# install without build tooling.
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-mcp-adapters>=0.1.0
acryl-datahub>=1.0.0
sqlglot>=25.0.0
jinja2>=3.1.0
pydantic>=2.7.0
typer>=0.12.0
rich>=13.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
httpx>=0.27.0
