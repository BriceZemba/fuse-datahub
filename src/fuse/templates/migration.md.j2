# Migration plan — {{ change.describe() }}

Generated by Fuse from DataHub lineage. Every asset below was found in the catalog,
not inferred from file names.

## What changes

- **Model:** `{{ change.model }}` (`{{ change.file }}`)
- **Change:** {{ change.describe() }}

## Who is affected

{% set actionable = impacts | rejectattr('severity', 'equalto', 'SAFE') | list %}
{% set quiet = impacts | selectattr('severity', 'equalto', 'SAFE') | list %}
{%- if actionable %}
| Asset | Type | Hops | Severity | Score | Evidence | Strategy |
|---|---|---|---|---|---|---|
{%- for i in actionable %}
| `{{ i.name }}` | {{ i.entity_type }} | {{ i.hops }} | **{{ i.severity }}** | {{ i.score }} | {{ i.evidence | join('; ') if i.evidence else '—' }} | {{ plan.get(i.urn, 'no_action') }} |
{%- endfor %}
{%- else %}
No consumer scored above SAFE.
{%- endif %}

{% if quiet %}<details><summary>{{ quiet | length }} further asset(s) scored SAFE</summary>

| Asset | Type | Hops | Score |
|---|---|---|---|
{%- for i in quiet %}
| `{{ i.name }}` | {{ i.entity_type }} | {{ i.hops }} | {{ i.score }} |
{%- endfor %}

</details>{% endif %}

## Order of operations

1. Merge the compatibility view(s) and contract tests **first**, while the old column still exists.
2. Notify the owners listed below and give them one release cycle to migrate.
3. Merge the column removal.
4. Run any backfill scripts in a maintenance window.
5. Delete the compatibility view once no consumer references it — Fuse tags it `fuse-deprecation` so it is easy to find.

## Owners to notify

{% for i in actionable %}{% if i.owners %}- `{{ i.name }}`: {{ i.owners | join(', ') }}
{% endif %}{% endfor %}{% if not actionable %}_Nothing scored above SAFE; no notification needed._{% endif %}

## Rollback

Revert the PR and run `fuse revert` to remove the tags and structured properties Fuse wrote to DataHub.
